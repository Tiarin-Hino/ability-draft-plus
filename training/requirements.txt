# Pinned combo known to work together (tf2onnx lags TensorFlow releases).
# Python 3.11 required (TF 2.15 does not support 3.12).
# The regression gate runs separately with requirements-gate.txt (modern ORT) —
# tf2onnx 1.16 pins an `onnx` version that makes newer onnxruntime
# ResolutionImpossible in this environment (verified).
tensorflow==2.15.1
tf2onnx==1.16.1
onnxruntime==1.18.1
# 1.14.0 hard-pins protobuf==3.20.2, which conflicts with TF's >=3.20.3;
# 1.16.0 declares protobuf unpinned and resolves cleanly (verified locally)
onnxconverter-common==1.16.0
numpy<2
protobuf~=3.20
